Where did the equation for the polarization density at the beginning get derived from?
$$
\vec{P} \equiv \left< n_{p} \vec{p} \right>
$$
Where $\vec{P}$ is the polarization density, $n_{p}$ it the number density and $\vec{p}$ are the microscopic dipoles

When deriving the surface polarization from the volume density, why do we not account for the field going through the top of our Gaussian volume? Why just the bottom side?

When deriving the spatial average of the macroscopic field, where does this dot product come from?
$$
E_{z} = \frac{1}{V} \sum_{i} \int \vec{E}_{mi}\cdot d\vec{l} \, da_{i}
$$
Where the differential is defined as a number of columns along an area $a$ (used to span some volume $r^{3}$).
- While on this topic, how come we know the field is something like $E\propto \sigma t /\epsilon_{0}$ where $t$ is thickness and $\sigma$ is the charge (or polarization?) density
